{{- define "CsUrlValuesFileHeader"}}// Code generated by https://github.com/ahfuzhang/BaoHuLu. DO NOT EDIT.
// source: {{.Namespace}}

#pragma warning disable CS8600, CS8601, CS8602, CS8618, CS8625

using System;
using System.Collections.Generic;
using System.Web;
using System.Text;
using System.Text.Json;
using QiWa.Common;

namespace {{.Namespace}};
{{end}}
{{- define "CsUrlValuesWriterFile"}}{{template "CsUrlValuesFileHeader" .}}
{{- with .Msg}}
{{- $goName := .GoName}}
// ============================================================================
// {{$goName}} — ToURLValues serialization (URL query-string format)
// ============================================================================

public partial struct {{$goName}}
{
    public readonly Error ToURLValues(ref RentedBuffer buf)
    {
        bool _first = true;
{{range .Fields}}
{{- if .IsMap}}
        if (this.{{.Name}} != null && this.{{.Name}}.Count > 0)
        {
            var _mapBuf{{.Name}} = new RentedBuffer(256);
            bool _mapFirst{{.Name}} = true;
            foreach (var _kv in this.{{.Name}})
            {
                if (!_mapFirst{{.Name}}) _mapBuf{{.Name}}.Append((byte)'&'); _mapFirst{{.Name}} = false;
{{- if eq .MapKeyCS "string"}}
                _mapBuf{{.Name}}.Append(_kv.Key ?? "");
{{- else if eq .MapKeyCS "bool"}}
                _mapBuf{{.Name}}.Append(_kv.Key ? "true"u8 : "false"u8);
{{- else if eq .MapKeyCS "long"}}
                _mapBuf{{.Name}}.Append(_kv.Key);
{{- else if eq .MapKeyCS "ulong"}}
                _mapBuf{{.Name}}.Append(_kv.Key);
{{- else}}
                _mapBuf{{.Name}}.Append((long)_kv.Key);
{{- end}}
                _mapBuf{{.Name}}.Append((byte)'=');
{{- if .UseMapValWrapper}}
                _mapBuf{{.Name}}.Append(JsonSerializer.Serialize(_kv.Value.Value));
{{- else if .MapValIsMsg}}
                _mapBuf{{.Name}}.Append(JsonSerializer.Serialize(_kv.Value));
{{- else if eq .MapValCS "string"}}
                _mapBuf{{.Name}}.Append(_kv.Value ?? "");
{{- else if eq .MapValCS "byte[]"}}
                _mapBuf{{.Name}}.Append(Convert.ToBase64String(_kv.Value));
{{- else if eq .MapValCS "bool"}}
                _mapBuf{{.Name}}.Append(_kv.Value);
{{- else if eq .MapValCS "double"}}
                _mapBuf{{.Name}}.Append(_kv.Value);
{{- else if eq .MapValCS "float"}}
                _mapBuf{{.Name}}.Append((double)_kv.Value);
{{- else if eq .MapValCS "long"}}
                _mapBuf{{.Name}}.Append(_kv.Value);
{{- else if eq .MapValCS "ulong"}}
                _mapBuf{{.Name}}.Append(_kv.Value);
{{- else if .MapValIsEnum}}
                _mapBuf{{.Name}}.Append((long)(int)_kv.Value);
{{- else}}
                _mapBuf{{.Name}}.Append((long)_kv.Value);
{{- end}}
            }
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(Uri.EscapeDataString(Encoding.UTF8.GetString(_mapBuf{{.Name}}.AsSpan())));
        }
{{- else if .IsRepeated}}
        if (this.{{.Name}} != null && this.{{.Name}}.Count > 0)
        {
{{- if .ElemIsMsg}}
            foreach (var _item{{.Name}} in this.{{.Name}})
            {
                var _subBuf{{.Name}} = new RentedBuffer(256);
                var _subErr{{.Name}} = _item{{.Name}}.ToURLValues(ref _subBuf{{.Name}});
                if (_subErr{{.Name}}.Err()) return _subErr{{.Name}};
                if (!_first) buf.Append((byte)'&'); _first = false;
                buf.Append({{$goName}}Tags.JsonKey{{.Name}});
                buf.Append((byte)'=');
                buf.Append(Uri.EscapeDataString(Encoding.UTF8.GetString(_subBuf{{.Name}}.AsSpan())));
            }
{{- else}}
            foreach (var _item{{.Name}} in this.{{.Name}})
            {
                if (!_first) buf.Append((byte)'&'); _first = false;
                buf.Append({{$goName}}Tags.JsonKey{{.Name}});
                buf.Append((byte)'=');
{{- if .IsString}}
                buf.Append(Uri.EscapeDataString(_item{{.Name}} ?? ""));
{{- else if .IsBytes}}
                buf.Append(Convert.ToBase64String(_item{{.Name}}));
{{- else if .IsBool}}
                buf.Append(_item{{.Name}});
{{- else if .IsEnum}}
                buf.Append((long)(int)_item{{.Name}});
{{- else if eq .ElemTypeCS "double"}}
                buf.Append(_item{{.Name}});
{{- else if eq .ElemTypeCS "float"}}
                buf.Append((double)_item{{.Name}});
{{- else if eq .ElemTypeCS "long"}}
                buf.Append(_item{{.Name}});
{{- else if eq .ElemTypeCS "ulong"}}
                buf.Append(_item{{.Name}});
{{- else}}
                buf.Append((long)_item{{.Name}});
{{- end}}
            }
{{- end}}
        }
{{- else if .IsMsg}}
{{- if .UseDirectWrapper}}
        if (this.{{.Name}} != null)
        {
            var _subBuf{{.Name}} = new RentedBuffer(256);
            var _subErr{{.Name}} = this.{{.Name}}.Value.ToURLValues(ref _subBuf{{.Name}});
            if (_subErr{{.Name}}.Err()) return _subErr{{.Name}};
            if (_subBuf{{.Name}}.AsSpan().Length > 0)
            {
                if (!_first) buf.Append((byte)'&'); _first = false;
                buf.Append({{$goName}}Tags.JsonKey{{.Name}});
                buf.Append((byte)'=');
                buf.Append(Uri.EscapeDataString(Encoding.UTF8.GetString(_subBuf{{.Name}}.AsSpan())));
            }
        }
{{- else}}
        {
            var _subBuf{{.Name}} = new RentedBuffer(256);
            var _subErr{{.Name}} = this.{{.Name}}.ToURLValues(ref _subBuf{{.Name}});
            if (_subErr{{.Name}}.Err()) return _subErr{{.Name}};
            if (_subBuf{{.Name}}.AsSpan().Length > 0)
            {
                if (!_first) buf.Append((byte)'&'); _first = false;
                buf.Append({{$goName}}Tags.JsonKey{{.Name}});
                buf.Append((byte)'=');
                buf.Append(Uri.EscapeDataString(Encoding.UTF8.GetString(_subBuf{{.Name}}.AsSpan())));
            }
        }
{{- end}}
{{- else if .IsBytes}}
        if (this.{{.Name}} != null && this.{{.Name}}.Length > 0)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(Convert.ToBase64String(this.{{.Name}}));
        }
{{- else if .IsBool}}
        if (this.{{.Name}})
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append("=true"u8);
        }
{{- else if .IsString}}
        if (!string.IsNullOrEmpty(this.{{.Name}}))
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(Uri.EscapeDataString(this.{{.Name}}));
        }
{{- else if .IsEnum}}
        if ((int)this.{{.Name}} != 0)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append((long)(int)this.{{.Name}});
        }
{{- else if .IsDecimal}}
        if (this.{{.Name}} != 0m)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append((double)Math.Round(this.{{.Name}}, {{.DecimalRound}}, MidpointRounding.AwayFromZero));
        }
{{- else if eq .WriterType "double"}}
        if (this.{{.Name}} != 0.0)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(this.{{.Name}});
        }
{{- else if eq .WriterType "float"}}
        if (this.{{.Name}} != 0.0f)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append((double)this.{{.Name}});
        }
{{- else if .IsFixed64}}
        if (this.{{.Name}} != {{csDefault .WriterType}})
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(this.{{.Name}});
        }
{{- else if eq .WriterType "long"}}
        if (this.{{.Name}} != 0)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(this.{{.Name}});
        }
{{- else if eq .WriterType "ulong"}}
        if (this.{{.Name}} != 0)
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append(this.{{.Name}});
        }
{{- else}}
        if (this.{{.Name}} != {{csDefault .WriterType}})
        {
            if (!_first) buf.Append((byte)'&'); _first = false;
            buf.Append({{$goName}}Tags.JsonKey{{.Name}});
            buf.Append((byte)'=');
            buf.Append((long)this.{{.Name}});
        }
{{- end}}
{{end}}
        return default;
    }
}
{{end}}{{end}}
{{- define "CsUrlValuesReadonlyFile"}}{{template "CsUrlValuesFileHeader" .}}
{{- with .Msg}}
{{- $goName := .GoName}}
// ============================================================================
// Readonly{{$goName}} — FromURLValues deserialization (URL query-string format)
// ============================================================================

public partial struct Readonly{{$goName}}
{
    public Error FromURLValues(ReadOnlySpan<byte> binary)
    {
        // Step 1: decode the URL query string into a collection using the standard library
        var _q = HttpUtility.ParseQueryString(Encoding.UTF8.GetString(binary));

        // Step 2: dispatch each key to its struct member
        foreach (string? _k in _q.Keys)
        {
            if (_k == null) continue;
            var _vals = _q.GetValues(_k);
            if (_vals == null || _vals.Length == 0) continue;
            switch (_k)
            {
{{range .Fields}}
                case "{{.JsonName}}":
{{- if .IsMap}}
                {
                    var _dict{{.Name}} = new {{.EffReadonlyType}}();
                    foreach (var _part in _vals[0].Split('&', StringSplitOptions.RemoveEmptyEntries))
                    {
                        var _meq = _part.IndexOf('=');
                        var _mk = _meq < 0 ? _part : _part.Substring(0, _meq);
                        var _mv = _meq < 0 ? "" : _part.Substring(_meq + 1);
                        {{csUrlKeyParse .MapKeyCS .JsonName}}
{{- if .UseMapValWrapper}}
                        var _mval{{.Name}} = JsonSerializer.Deserialize<{{.ReadonlyMapValCS}}>(_mv);
                        _dict{{.Name}}[_key] = new {{.WrapReadonlyMapValCS}} { Value = _mval{{.Name}} };
{{- else if eq .MapValCS "string"}}
                        _dict{{.Name}}[_key] = _mv;
{{- else if eq .MapValCS "byte[]"}}
                        _dict{{.Name}}[_key] = Convert.FromBase64String(_mv);
{{- else if eq .MapValCS "bool"}}
                        _dict{{.Name}}[_key] = _mv == "true" || _mv == "1";
{{- else if .MapValIsMsg}}
                        _dict{{.Name}}[_key] = JsonSerializer.Deserialize<{{.ReadonlyMapValCS}}>(_mv);
{{- else if .MapValIsEnum}}
                        if (!int.TryParse(_mv, out int _mvi{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _dict{{.Name}}[_key] = ({{.ReadonlyMapValCS}})_mvi{{.Name}};
{{- else}}
                        if (!{{.MapValCS}}.TryParse(_mv, out {{.MapValCS}} _mvi{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _dict{{.Name}}[_key] = _mvi{{.Name}};
{{- end}}
                    }
                    this.{{.Name}} = _dict{{.Name}};
                    break;
                }
{{- else if .IsRepeated}}
                {
                    var _list{{.Name}} = new {{.LocalType}}(_vals.Length);
                    foreach (var _sv in _vals)
                    {
{{- if .ElemIsMsg}}
                        var _sub{{.Name}} = new {{.ReadonlyElemTypeCS}}();
                        var _err{{.Name}} = _sub{{.Name}}.FromURLValues(Encoding.UTF8.GetBytes(_sv));
                        if (_err{{.Name}}.Err()) return _err{{.Name}};
                        _list{{.Name}}.Add(_sub{{.Name}});
{{- else if .IsBytes}}
                        _list{{.Name}}.Add(Convert.FromBase64String(_sv));
{{- else if .IsBool}}
                        _list{{.Name}}.Add(_sv == "true" || _sv == "1");
{{- else if .IsString}}
                        _list{{.Name}}.Add(_sv);
{{- else if .IsEnum}}
                        if (!int.TryParse(_sv, out int _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(({{.ReadonlyElemTypeCS}})_el{{.Name}});
{{- else if eq .ElemTypeCS "double"}}
                        if (!double.TryParse(_sv, out double _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(_el{{.Name}});
{{- else if eq .ElemTypeCS "float"}}
                        if (!float.TryParse(_sv, out float _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(_el{{.Name}});
{{- else if eq .ElemTypeCS "long"}}
                        if (!long.TryParse(_sv, out long _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(_el{{.Name}});
{{- else if eq .ElemTypeCS "ulong"}}
                        if (!ulong.TryParse(_sv, out ulong _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(_el{{.Name}});
{{- else if eq .ElemTypeCS "uint"}}
                        if (!uint.TryParse(_sv, out uint _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(_el{{.Name}});
{{- else}}
                        if (!int.TryParse(_sv, out int _el{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                        _list{{.Name}}.Add(_el{{.Name}});
{{- end}}
                    }
                    this.{{.Name}} = _list{{.Name}};
                    break;
                }
{{- else if .IsMsg}}
{{- if .UseDirectWrapper}}
                {
                    var _subInner{{.Name}} = new {{.ReadonlyType}}();
                    var _err{{.Name}} = _subInner{{.Name}}.FromURLValues(Encoding.UTF8.GetBytes(_vals[0]));
                    if (_err{{.Name}}.Err()) return _err{{.Name}};
                    this.{{.Name}} = new {{.EffReadonlyType}} { Value = _subInner{{.Name}} };
                    break;
                }
{{- else}}
                {
                    var _sub{{.Name}} = new {{.ReadonlyType}}();
                    var _err{{.Name}} = _sub{{.Name}}.FromURLValues(Encoding.UTF8.GetBytes(_vals[0]));
                    if (_err{{.Name}}.Err()) return _err{{.Name}};
                    this.{{.Name}} = _sub{{.Name}};
                    break;
                }
{{- end}}
{{- else if .IsBytes}}
                    this.{{.Name}} = Convert.FromBase64String(_vals[0]);
                    break;
{{- else if .IsBool}}
                    this.{{.Name}} = _vals[0] == "true" || _vals[0] == "1";
                    break;
{{- else if .IsString}}
                    this.{{.Name}} = _vals[0];
                    break;
{{- else if .IsEnum}}
                    if (!int.TryParse(_vals[0], out int _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = ({{.ReadonlyType}})_p{{.Name}};
                    break;
{{- else if .IsDecimal}}
                    if (!double.TryParse(_vals[0], out double _pd{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = (decimal)_pd{{.Name}};
                    break;
{{- else if eq .WriterType "double"}}
                    if (!double.TryParse(_vals[0], out double _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = _p{{.Name}};
                    break;
{{- else if eq .WriterType "float"}}
                    if (!float.TryParse(_vals[0], out float _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = _p{{.Name}};
                    break;
{{- else if eq .WriterType "long"}}
                    if (!long.TryParse(_vals[0], out long _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = _p{{.Name}};
                    break;
{{- else if eq .WriterType "ulong"}}
                    if (!ulong.TryParse(_vals[0], out ulong _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = _p{{.Name}};
                    break;
{{- else if eq .WriterType "uint"}}
                    if (!uint.TryParse(_vals[0], out uint _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = _p{{.Name}};
                    break;
{{- else}}
                    if (!int.TryParse(_vals[0], out int _p{{.Name}})) return Error.WithLoc(1, "bad value {{.JsonName}}");
                    this.{{.Name}} = _p{{.Name}};
                    break;
{{- end}}
{{end}}
            }
        }
        return default;
    }
}
{{end}}{{end}}
